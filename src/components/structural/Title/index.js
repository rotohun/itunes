import React from 'react';
import {Text} from 'react-native';

const Title = () => {
  return (
    <Text
      style={{
        textAlign: 'center',
        fontSize: 30,
        fontWeight: 'bold',
        marginVertical: 20,
      }}>
      Top 100 Albums
    </Text>
  );
};

export default Title;
